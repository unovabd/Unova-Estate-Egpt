import DemoClient from '@/components/DemoClient';

export const metadata = {
  title: 'Book a Demo — Unova Estate',
  description: 'Schedule a live 30-minute personalized demo of Unova Estate with our product experts.',
};

export default function DemoPage() {
  return <DemoClient />;
}
