import ResourcesClient from '@/components/ResourcesClient';

export const metadata = {
  title: 'Resource Center — Unova Estate',
  description: 'Find documentation, blog articles, guides, calculators, and templates to optimize your real estate operations.',
};

export default function ResourcesPage() {
  return <ResourcesClient />;
}
